# History

## 2026-07-15

- 08:23:09 +08:00: Completed the Task 16 executable route-scoped required-state audit. `productDesign.requiredStates` now requires `name/trigger/route/steps/expectText`; Planner and Builder prompts describe the same deterministic protocol; the Playwright UI collector keeps ordinary page audits state-free, then independently navigates each state route, performs click/fill steps, verifies visible evidence on desktop and mobile, captures state screenshots, and feeds failures through the existing reviewer/fixer/report gate. Focused tests pass 83/83; the default suite passes 114 with 19 opt-in skips. A full no-API E2E run exercised all 11 production scenarios: ten passed immediately, the remaining scenario failed only on the old two-screenshot assertion after state evidence expanded the expected count to six, and the corrected targeted scenario passed. Two fresh real `gpt-5.5` SignalDesk attempts then failed before Planner completion on upstream HTTP 503; minimal probes for `gpt-5.5`, `gpt-5.6-luna`, and `gpt-5.6-sol` all returned `Service temporarily unavailable`. No key, endpoint, failed report, or screenshot was published; rerun SignalDesk before Atlas when the inference plane recovers.
- 10:48:23 +08:00: Resumed Task 16 with a temporary user-supplied credential. The gateway listed seven models but enforced `channel:client_restricted` for the default Node/curl clients; five approved-client UA probes passed, and the optional process-only `VIBE_ONE_USER_AGENT` configuration was added with regression coverage. `gpt-5.5` SignalDesk then completed successfully after one repair round: 3 pages, 6/6 interactions, 28 verification checks, route-scoped loading/empty/error/success evidence on both viewports, and a passing bounded polish candidate. The sanitized report and three final screenshots were copied to `docs/demo-reports/signaldesk.md` and `docs/screenshots/signaldesk-*.png`. Atlas was attempted with `gpt-5.5` and `gpt-5.6-luna`; both failed visual/UI quality gates after bounded repair, so no Atlas failure artifacts were published. Credentials and endpoint were not persisted.
- 12:10:15 +08:00: Expanded the published SignalDesk showcase with three curated evidence images from the successful run: mobile queue, completed-review interaction result, and mobile empty state. README and HANDOFF now expose six screenshots total without publishing the full internal audit bundle.
- 13:29:44 +08:00: Completed the bilingual portfolio presentation pass. README remains Chinese-first but now gives English readers an early product overview, verified SignalDesk metrics, honest Atlas status, bilingual section headings, and direct evidence links; the SignalDesk report adds an English executive summary and evidence table; Architecture and HANDOFF add Chinese summaries. Link audit found zero missing local targets, public secret/endpoint scan found zero matches, and the default suite remains 114 passed / 19 opt-in skipped / 0 failed. The product is ready to present through SignalDesk; Atlas remains explicitly outside the successful showcase claim.

## 2026-07-14

- 22:58:19 +08:00: Task 16 advanced from API outage to a verified real-model quality-gate result using a temporary user-supplied OpenAI-compatible credential that was never persisted or published. `gpt-5.6-sol` repeatedly generated 15k-17k completion-token Builder responses beyond the 12-file / 24k-character hard contract, and the gateway ignored both `max_tokens` and `max_completion_tokens`; Builder now targets 18k characters / six files and supplies a 6,000-token cap for compliant gateways, with core tests passing 73/73. `gpt-5.6-luna` produced a valid four-file app and completed build/screenshots but exhausted two repairs with three failed scenarios. `gpt-5.5` produced a valid four-file app, passed all 6/6 scenarios through both repair rounds, and reduced the remaining review failure to UI quality only; the terminal audit still failed because product-level loading/empty/error/success states are repeated as visible requirements across every page and viewport. SignalDesk and Atlas showcase evidence remain unpublished; next engineering step is executable route-scoped state evidence before any further paid run.
- 21:08:32 +08:00: Retried Task 16 with another user-supplied alternate credential without persisting or publishing its key or endpoint. Authentication succeeded, but `/v1/models` returned an empty `data` list and both OpenAI Chat Completions and Anthropic Messages returned `model_not_found`: the credential's `Kiro` group has no distributor/channel for `claude-opus-4-8`. Common Opus 4.8 model aliases also had no channel. This credential cannot perform inference until its provider binds a model distributor or issues a key whose authenticated model list is non-empty; no token usage or showcase evidence was produced.
- 20:44:28 +08:00: Retried Task 16 with a user-supplied alternate OpenAI-compatible credential without persisting or publishing the key or endpoint. Authenticated model discovery succeeded and exposed the required `gpt-5.6-sol`, while unauthenticated and fake-key discovery correctly returned 401; however, both `/chat/completions` and `/responses` were rejected by the gateway nginx layer with HTTP 403 under PowerShell, an OpenAI-style User-Agent, and Vibe-one's own Node provider. The failure occurs before any model/token usage, so Task 16 remains externally blocked by inference-route permission, IP policy, or WAF configuration rather than local code.
- 20:30:38 +08:00: Task 16 real-API retry remains externally blocked. A fresh SignalDesk plan probe exhausted all six bounded retries on HTTP 503. The authenticated `/models` control-plane check returned 200 and listed the configured model, but minimal 8-token inference probes returned 503 for `gpt-5.6-sol`, `gpt-5.4`, `gpt-5.6`, `gpt-5.5`, `gpt-5.4-mini`, and `gpt-5.2-chat-latest`; the `/responses` path also returned 503. This isolates the failure to the gateway inference plane rather than credentials, model discovery, local code, or the second Codex window. No alternate OpenAI/Anthropic/Azure API environment is configured, and no showcase evidence was fabricated.
- 19:23:05 +08:00: Product Studio quality redesign Task 17 completed. Reframed the README Chinese-first around Product Studio, the bounded quality pipeline, and representative SignalDesk/Atlas demos; moved Expense/Notes material to `docs/demos/archive.md`; refreshed framework, architecture, and handoff docs; and added deterministic 1440x900 desktop, 390x844 mobile, and Focus screenshots through `scripts/readme-screenshots.mjs`. Final verification exposed and fixed a Windows Node filesystem false positive where `FileHandle.stat()` reported a volume id while `lstat()` reported `dev=0` for the same inode; secure evidence reads now accept that exact same-inode case while swap, symlink, and hard-link protections remain green. Verification: console tests 26/26, default suite 114 passed / 19 opt-in skipped / 0 failed, browser screenshot E2E 10/10, serial no-API opt-in `test/e2e.test.js` passed, and whitespace checks passed. Task 16 remains externally blocked: SignalDesk, Atlas, and repeated planner probes exhausted all bounded retries on upstream HTTP 503, so no showcase success artifacts were fabricated.
- 14:15:49 +08:00: Product Studio quality redesign Task 15 completed. Added public-safe representative inputs for SignalDesk (text-only AI customer-service QA/operations SaaS) and Atlas Research (brief plus two ordered reference images), each with exactly three product pages, six mechanically testable interactions, realistic internally consistent mock-data requirements, and loading/empty/error/success states. Added `demo:signaldesk` and `demo:atlas` scripts; Atlas reference discovery validates both PNGs through the existing manifest/magic-byte/dimension jail. Verification: focused RED failed on the missing SignalDesk brief, then the representative-input contract passed and full `core.test.js` passed 72/72; public input secret/private-path scan and whitespace checks passed.
- 13:44:59 +08:00: Product Studio quality redesign Task 14 completed. Below 900px the product canvas remains the primary Flow surface while the production timeline and quality Inspector become mutually exclusive left/right drawers with Escape close and focus return; closed drawers no longer expand the document width. Canvas page/device controls and every visible button/link/input/select now meet the 44px target, motion remains 220ms with the existing reduced-motion override, and 390px has no horizontal overflow. Verification: console browser E2E passed 10/10, `console.test.js` passed 25/25, syntax and whitespace checks passed. The concurrent Task 13 preview-route follow-up `0e1cc36` was preserved.
- 09:19:47 +08:00: Task 13 follow-up fixed the selected Canvas page being lost on first preview launch; choosing a mapped route now opens that route immediately without restarting the preview server. Browser E2E remains 9/9.
- 09:07:09 +08:00: Product Studio quality redesign Task 13 completed. Flow is now a semantic three-part production workspace: an event-derived eight-stage production timeline, a dominant product canvas with desktop/tablet/mobile switching and persistent preview controls, and a quality Inspector with Product Spec, Design System, UI Quality, and Evidence views. The new `studio-renderers.js` keeps timeline/Inspector DOM rendering separate from fetch/EventSource ownership, and reads only the existing sanitized design/quality/polish APIs. Verification: console browser E2E passed 9/9 including desktop/mobile history replay, landmarks, viewport switching and evidence tabs; `console.test.js` passed 25/25; syntax and whitespace checks passed.

## 2026-07-13

- 23:12:54 +08:00: Product Studio quality redesign Task 12 completed. Focus now captures product goal, target users, at most three core flows, visual direction, optional constraints, and three bounded product presets; uploaded references render as an ordered storyboard with editable page roles, and their order/roles are composed into the submitted brief without weakening the existing upload limits or session settings. TDD evidence: the new browser contract failed on the missing product-goal control, then console E2E passed 9/9 at desktop/mobile sizes; app and reference-input syntax checks passed.
- 20:34:31 +08:00: Product Studio quality redesign Task 11 completed with a pure Studio state reducer and one event-replay path shared by persisted job history and live SSE updates. The console keeps fetch/EventSource/DOM ownership in `app.js`, serves `studio-state.js` as same-origin JavaScript under the existing CSP, and does not begin the visual redesign. TDD evidence: the two new contracts failed for the missing module/static route, then `console.test.js` passed 24/24; the full default suite passed 110 with 17 opt-in skips, and browser console E2E passed 8/8.
- 19:03:18 +08:00: Product Studio quality redesign Task 10 completed after TDD, independent specification review, and two independent code-quality review rounds. The bilingual Delivery Report now persists safe design/UI/visual/polish sidecars plus every referenced UI/polish/draft raster in one atomic immutable bundle; marker-mode APIs never fall back to mutable root evidence, while legacy no-marker runs remain readable. Publication rejects linked, hard-linked, escaped, or raced files; missing raster stays a stable 404 instead of later reviving from mutable storage. Commits before final hardening: `d6a5091`, `e4b3eae`, `20bd20d`; final verification: Task 10 core/console 93/93 and default suite 108 pass / 17 opt-in skip / 0 fail.
- 09:24:46 +08:00: Reconstructed the Vibe-one product line from the live repository for interview understanding. The implemented product at that time was the local Product Lab and bounded React/Vite delivery pipeline; Product Studio, deterministic UI-quality audit, polish, and richer SignalDesk/Atlas demos were still approved design/plan work and were not to be presented as implemented before landing.
- 09:22:08 +08:00: Began Vibe-one interview preparation for Gilbert's Java-backend internship interviews. Interview material should use natural spoken Chinese that can be said directly in a real conversation, not formal project-defense prose; prioritize likely follow-up chains, personal contribution, real failures, engineering safeguards, and honest product boundaries.
- 08:28:20 +08:00: Task 9 completed: one bounded polish pass is integrated into the pipeline only after the draft is fully green; the candidate must pass build, content, interaction, UI, and visual re-verification before promotion, and terminal evidence plus rollback-recovery flags remain consistent on failure (`0813bd1`, `094017b`).

## 2026-07-12

- 20:50:26 +08:00: Task 8 completed: one model polish call writes only bounded UI changes into an isolated candidate; source sanitization preserves legitimate app paths/URLs while removing private values, and screenshot inputs are jailed, link-rejected, magic-byte checked, MIME-verified, and size/count bounded (`94e574c`, `4e34cbf`).
- 19:25:08 +08:00: Task 7 completed: bounded polish candidate lifecycle and promotion were isolated from the final app; copy rejects symlink/junction sources and promotion exposes rollback failure instead of silently losing the app (`bfe0029`, `662c623`).
- 17:45:40 +08:00: Task 6 completed: UI audit is a real reviewer/fixer/pipeline delivery gate with bounded repair evidence; collector infrastructure failures stay fatal, per-round screenshots remain immutable, and fixer attachments are count/byte bounded (`86a15bb`, `3cb493d`).
- 15:08:40 +08:00: Task 5 completed: Playwright collects ordered desktop/mobile DOM and screenshot evidence from the loopback preview only, excludes inherited hidden/disabled content, aggregates only truly visible text, and rejects cross-origin routes (`89e6e4c`, `20a6a9b`, `8cfcc60`).
- 13:03:53 +08:00: Task 4 completed: deterministic pure UI audit rules and stable failure codes cover viewport, density, interaction targets, content, and WCAG AA contrast with correct large-text boundaries and explicit translucent-backdrop composition (`3bdc5e7`, `ab0f5f1`, `b73d23a`).
- 09:41:13 +08:00: Task 3 completed: Builder enforces the 12-file / 24k output budget, mature UI contract, fixed dependency whitelist including `lucide-react`, and rejects oversized real `build()` output before model-authored files are written (`2ab4452`, `e691bb4`).
- 09:02:46 +08:00: Task 2 completed: Planner validates executable product design, writes Chinese-first bilingual SPEC/PLAN artifacts including the product summary, and tells the model the same exact token/state thresholds enforced locally (`e84f43e`, `c2a879f`, `fbe2672`).
- 07:17:13 +08:00: Task 1 completed: executable Product Design contract, validation, and Chinese-first bilingual rendering landed with distinct-state enforcement, rejection of generic-only tones, and support for concise valid labels (`e45fb98`, `e6ec173`, `7e32fd7`).

## 2026-07-09

- 19:35:08 +08:00: Drafted resume-ready Chinese project content for Vibe-one in the style of Gilbert's target resume template. Positioning used: AI product delivery pipeline / local Product Lab / text+screenshot input / React+Vite generation / Playwright functional verification / deterministic visual gate / bounded repair / auditable reports. No repository code changed.
- 19:40:00 +08:00: Condensed the Vibe-one resume entry to three strongest bullets for resume space: AI orchestration/product generation, mechanical verification/visual gate, and bounded repair/auditable evidence chain. No repository code changed.
- 19:44:00 +08:00: Rewrote the three-bullet Vibe-one resume entry in Gilbert's preferred bold-label style matching the Urban-Pulse/Heima Dianping project format. No repository code changed.
- 22:25:19: Created the initial `FRAMEWORK.md` for Vibe-one. Project positioning: a hands-off app replication pipeline that uses a single OpenAI-compatible API plus local scripts to turn product briefs/screenshots into runnable app artifacts with generated specs, verification, bounded repair loops, screenshots, token/cost tracking, and delivery reports. The first milestone should use text briefs before screenshot recognition.
- 23:09:20: Collaboration decision: it is reasonable to let Fable5 rapidly scaffold the framework code from `FRAMEWORK.md`, while Codex should act as reviewer/architect for scope control, command/API/output contracts, verification, and repair-loop safety. Fable5 should not invent screenshot recognition or multi-provider complexity for the first pass.
- 23:5x: Fable5 scaffolded the Phase 1 MVP framework code (commit `1d08d8f`, 24 files): `src/cli` entry, `src/core` (config/runContext/pipeline/planner/builder/reviewer/fixer), single OpenAI-compatible provider in `src/providers`, `src/runner` (spawn+capture, vite preview wait, Playwright screenshots), `src/reporter` delivery report, two example briefs (expense-mobile, notes-mobile), docs (architecture, interview-positioning), and 5 offline unit tests (all passing, `npm test`). Safety contracts in place: `safeJoin` path jail for model-written files, bounded repair rounds, mechanical-only reviewer, per-command timeouts, token usage accounting. Env contract: `VIBE_ONE_API_KEY` / `VIBE_ONE_BASE_URL` / `VIBE_ONE_MODEL`. Not yet done: real API run (needs key), `npm install` of playwright, remote repo (git initialized locally on `main`, no origin yet).
- 23:49:46: Codex reviewed Fable5's Phase 1 scaffold against the Vibe-one framework. Verified `origin` now points to `https://github.com/Aschenbath/Vibe-one.git`, `npm test` passes 5/5 offline tests, and UTF-8 Chinese docs/examples render correctly when read with explicit UTF-8. Review conclusion: the scaffold direction is good but not interview-ready yet; top gaps are unsafe execution of model-generated `package.json` scripts during install/build, shallow reviewer checks that do not validate generated acceptance criteria or interactions, fixed preview/run-directory assumptions, and untracked `package-lock.json`. No real API pipeline run was performed.

## 2026-07-11

- 23:36:38 +08:00: Completed and self-reviewed the implementation plan for the approved Product Studio + quality-pipeline redesign: `docs/superpowers/plans/2026-07-11-product-studio-quality-redesign.md`. The plan contains 17 TDD tasks / 85 tracked steps covering executable `productDesign`, a 12-file/24k builder contract, deterministic desktop/mobile UI audit, one isolated 4-file/18k polish candidate with full re-verification, Product Studio Focus/Flow UI, safe evidence APIs, SignalDesk and Atlas Research real-model demos, and final documentation/evidence verification. Gilbert clarified that this GitHub portfolio must be Chinese-first bilingual for interviewers: public docs use a complete Chinese narrative plus concise English overview/evidence, while the console remains Chinese and stable technical identifiers remain English. No production code changed in this planning task.

- 21:23:45 +08:00: Gilbert approved the Product Studio + quality-pipeline redesign direction. Added `docs/superpowers/specs/2026-07-11-product-studio-quality-redesign.md`, defining the three-pane Studio workspace, structured product-design contract, deterministic UI quality audit, one bounded polish candidate, and replacement of the low-complexity README demos with SignalDesk and Atlas Research. No implementation code changed; the written spec remains the approval gate before planning.

- 20:39:23 +08:00: Rewrote the repository `README.md` in Chinese while preserving the verified Product Lab capabilities, commands, security boundaries, demo evidence, artifact paths, and current product limits. Validation: `git diff --check` and the public secret/private-endpoint scan passed; pushed the documentation update to `origin/main`.

- 20:00:09 +08:00: Completed and integrated the 15-task Product Lab visual-input phase on `main` at `d54fe8f`. The all-Chinese Focus -> Flow console now accepts text, screenshots, or both; requires valid planner mappings for every uploaded reference; applies deterministic local SSIM + color-histogram delivery gates; preserves immutable per-round visual evidence; performs bounded visual repair; and keeps API keys, private endpoints, base64 payloads, and absolute paths out of public jobs, events, reports, and DOM. Final merged-result verification: default suite 62 total / 52 pass / 10 opt-in skips; console Playwright 8/8; full stub pipelines 5/5 covering normal success, build repair, visual round-0 pass, and visual failure -> repair -> round-1 pass. Desktop/mobile screenshots were visually inspected and byte-reproducible. Remaining product boundaries: responsive React + Vite Web only, loopback-local console, one active job, session-only credentials, no remote hosting/auth, no concurrent jobs, and no mid-command cancellation.

- 15:28:15 +08:00: Completed Task 14 documentation and Product Lab UI evidence in `19b17bf`: aligned README/FRAMEWORK/architecture/HANDOFF with text-only, screenshot-only, and combined input; PNG/JPEG/WebP limits (4 files, 6 MiB each, 18 MiB total; 26 MiB console request); OpenAI-compatible multimodal planning; structured product/visual spec; deterministic local SSIM + color-histogram gate; bounded visual repair; coarse default threshold `0.62`; and session-only credential/base64/private-path boundaries. Updated the deterministic E2E screenshot fixture to use a Chinese task/preview and populated desktop/mobile preview states, then replaced screenshots from 1440×900 and 390×844 viewports; final coordinated image inspection found no clipping, overlap, empty preview, English fixture, or narrow mobile Flow. Fresh verification: `npm test` 59 total / 49 pass / 10 opt-in skips; console E2E 8/8; `VIBE_ONE_E2E=1 npm test` 59 total / 53 pass / 6 console skips, including visual round-0 pass and visual failure -> repair -> round-1 pass; four syntax checks and `git diff --check` passed; the required secret/base64/private-endpoint scan had no matches. Remaining boundaries: no pixel-perfect claim, remote hosting/auth, concurrent jobs, durable credentials, or mid-command cancellation.

## 2026-07-10

- 19:29:36: Completed and pushed the implementation plan for the approved Product Lab visual-input phase: `docs/superpowers/plans/2026-07-10-product-lab-visual-input.md` (15 TDD tasks across trusted reference ingestion, multimodal planning, deterministic visual comparison/repair, Chinese Focus -> Flow console, evidence/docs/handoff), commit `c3cf276`. During plan work, diagnosed the repository's long-running Linux CI failures from real Actions evidence: Node 20 treated the quoted `test/*.test.js` glob as a literal path, and POSIX `path.isAbsolute()` treated `C:\Windows\evil.js` as relative. Added a Node-20-compatible explicit test command plus regression contract (`cc9ded8`) and cross-platform absolute-path detection using both `path.posix` and `path.win32` (`5d718fa`). Verification: Node 20 and Node 22 each pass 32 tests with 3 opt-in skips; GitHub Actions runs for `5d718fa` and `c3cf276` both completed successfully. The Actions v4 Node-20 deprecation annotation remains a non-blocking warning, not a test failure.
- 17:09:37: Gilbert approved the Vibe-one Product Lab redesign direction: all-Chinese UI, `Focus -> Flow` workspace, desktop-first operator console, and real text/screenshot-to-responsive-Web-product generation. Added and self-reviewed `docs/superpowers/specs/2026-07-10-product-lab-visual-input-design.md`, covering bounded image ingestion, OpenAI-compatible multimodal planning, page/reference mapping, deterministic SSIM+color-histogram checks, visual repair evidence, security, tests, and explicit scope boundaries. Added `.superpowers/` to `.gitignore`; committed and pushed design baseline `e25bf3f`. No implementation code changed yet because the written-spec review gate remains pending.
- 14:36:38: Added the Vibe-one local browser console over the existing pipeline. The loopback-only Node HTTP/SSE server supports brief input, Full Run/Plan Only, session-memory credentials, one active job, live events, persisted run history, jailed screenshots/reports, and one owned generated-app preview. Added responsive desktop/mobile workstation UI and committed console screenshots. Verification: offline suite 30 pass / 3 opt-in skips; console Playwright e2e 3/3 pass at 1440x900 and 390x844; full stub pipeline suite 32 pass / 1 console-only skip, including normal success and failed-then-repaired npm/Vite/Playwright runs. Deferred intentionally: remote access/auth, concurrent jobs, durable secrets, and mid-command cancellation.
- 09:45:57: Codex resumed the Fable5 handoff and found the no-API e2e failed on this Windows shell because `os.tmpdir()` returned the 8.3 alias `C:\\Users\\ASCHEN~1`, while Vite/Rollup canonicalized it to the long path and rejected `index.html` as outside the project root. Updated `test/e2e.test.js` to canonicalize the temporary root with `fs.realpath()`; verified the full stub pipeline passes 14/14 without overriding `TEMP`. Real expense/notes runs remain blocked only by the missing `VIBE_ONE_API_KEY`.
- 11:55:07: Codex completed the Fable5 handoff for the text-brief MVP. Real `gpt-5.6-sol` runs succeeded for expense (round 0, 3 pages, 4 scenarios) and notes (round 1 after fixer patched `src/App.jsx`, 2 pages, 2 scenarios). Added streaming Chat Completions with JSON fallback, bounded 500/502/503/504 retries, a separate 10-minute stream timeout, an 8-file/12k builder budget, correct planner-only exit codes, a deterministic repair e2e, `demo:notes`, committed reports under `docs/demo-reports/`, and 8 screenshots under `docs/screenshots/`. Updated README/FRAMEWORK/HANDOFF/architecture/interview docs; verified the offline suite (18 pass, 2 e2e skipped) and full e2e suite (20/20 pass). No API key or private endpoint is stored in the repository.
- Fable5 closed Codex review round 1 (all five points) and hardened the pipeline before any real API run. (1) Arbitrary-script-execution hole closed: `builder.js` now writes a FIXED `package.json` + `vite.config.js` scaffold, refuses model writes to manifest/lockfile/npmrc/config (`FORBIDDEN_FILES` + `node_modules/`), pins a dependency whitelist (react/react-dom/react-router-dom), and `runner` installs with `--ignore-scripts`. (2) Reviewer deepened from shape-only to acceptance: planner now emits per-page `mustContain` text + structured `scenarios` (click/fill steps + expectText); `runner.runScenarios` drives them via Playwright; `reviewer` asserts mustContain fragments and scenario pass/fail, so a page of random text no longer passes. (3) `runContext` resolves `runs/` from project root via `import.meta.url` with `VIBE_ONE_RUNS_DIR`/`constraints.runsRoot` override. (4) Preview binds an OS-assigned free port (`getFreePort`) and kills the whole process tree via `taskkill /T` on Windows. (5) `package-lock.json` committed. Provider gained bounded 429 backoff. Tests grew 5 -> 8, all passing offline. Wrote `docs/HANDOFF.md` for Codex: done/not-done, predicted failure modes, working endpoint (`https://api.123nhh.com/v1`, default model `claude-opus-4-8` after smoke tests; sonnet-4/gpt-5.x variants incompatible or rate-limited on this gateway), and guardrails not to regress. Still NOT done: no live model call has ever run the pipeline end-to-end (biggest remaining unknown), no failed-then-repaired demo captured, notes-mobile unproven. Recommendation recorded: run `plan` (one cheap call) before full demo to validate real-model JSON shape.
- Fable5 added a no-API end-to-end integration test (`test/e2e.test.js`, opt-in via `VIBE_ONE_E2E=1`) that drives the ENTIRE pipeline with a stub provider returning a fixed spec + known-good React app: real `npm install --ignore-scripts`, real `vite build`, real preview on a free port, real Playwright screenshot + interaction scenario (click Add -> assert new row), reviewer pass, report written. Verified passing (`status=success`). This narrows the last unknown from "does the whole pipeline work" to just "does the real model return schema-matching JSON". The e2e test immediately caught a real bug: Node >=20.12 throws `EINVAL` spawning `.cmd` shims with `shell:false` (CVE-2024-27980 fix); `runner/commands.js` now shells npm calls (both runCommand and startPreview) with a single pipeline-controlled literal string. `runPipeline` now accepts an injected `provider` for testing. Full suite 10/10 (9 offline + 1 e2e); default `npm test` stays offline (~2s, e2e skipped). README + HANDOFF updated.
- Fable5 ran the pipeline against a REAL model (opus-4-8 via `https://api.123nhh.com/v1`) for the first time and fixed three hard bugs only real runs expose, each with a regression test (suite 13 offline + 1 e2e, all green): (a) **JSON-string file transport corrupts large output** — builder/fixer forced the model to escape whole source files into one JSON string, which broke `JSON.parse` at ~22KB (fatal, zero files). Switched both to a delimiter protocol (`=== FILE: path` / `=== END ===`, `parseFileBlocks`); the builder then successfully generated a 24-file app. (b) **Blind repair loop** — fixer saw only the build error text and guessed wrong (misdiagnosed a missing export as syntax error, then fixed one of two same-class missing exports); now `fix()` sends the current model-authored source via `gatherSource` (skips node_modules + fixed scaffold, bounded 40KB). (c) **Transient network failures killed runs** — provider now retries `fetch failed`/timeout with exponential backoff (default 6 attempts, per-request `AbortSignal.timeout`, configurable via `VIBE_ONE_MAX_RETRIES`/`VIBE_ONE_REQUEST_TIMEOUT_MS`) alongside the existing 429 handling.
- Real end-to-end SUCCESS not yet captured: the `api.123nhh.com` gateway was unstable for hours (probe pattern: small requests 200, but the long build-response call repeatedly hit multi-minute windows of `fetch failed` + 429 rate-limits that exhausted even 6 retries — runs 3/4/5/6 all failed at the build model call for this external reason, NOT a code fault). Planner stage succeeded repeatedly (real Chinese acceptance criteria + scenarios). Screenshots therefore not yet added to docs/README. Next session: when the gateway is healthy (or with a fresh non-shared key), `npm run demo:expense` should complete — the pipeline is otherwise proven by the offline e2e run. Then copy `runs/<id>/screenshots/*.png` into `docs/screenshots/` and embed in README. Repo hygiene added this session: MIT LICENSE, GitHub Actions CI (`.github/workflows/ci.yml`, offline tests), `.gitattributes` (LF normalization), README CI badge.
